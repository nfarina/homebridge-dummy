{
    "pluginAlias": "DummyAccessory",
    "pluginType": "accessory",
    "singular": false,
    "schema": {
        "type": "object",
        "properties": {
            "name": {
                "title": "Name",
                "type": "string",
                "required": true
            }, 
             "type": {
                "title": "Accessory Type",
                "type": "string",
                "oneOf": [
                 {
                 "title": "Switch",
                    "enum": ["Switch"]
               },
                  {
                   "title": "Outlet",
                    "enum": ["Outlet"]
                  },
                 {
                   "title": "Light",
                 "enum": ["Light"]
                 }
               ]
           },
            "stateful": {
                "title": "Stateful",
                "type": "boolean",
                "default": false,
                "description": "The outlet remains on instead of being automatically turned off."
            },
            "reverse": {
                "title": "Reverse",
                "type": "boolean",
                "default": false,
                "description": "The outlet's default state is on."
            },
            "time": {
                "title": "Time",
                "type": "number",
                "default": 1000,
                "description": "The outlet will turn off after this number of milliseconds. Not used if the outlet is stateful."
            },
            "resettable": {
                "title": "Resettable",
                "type": "boolean",
                "default": false,
                "description": "The timer will reset each time the outlet is turned on. "
            }
        }
    }
}
